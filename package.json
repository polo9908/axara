{
  "name": "a11yengine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Hybrid (static + runtime) accessibility & design-system audit engine — RGAA 4.1.2 / 5.0",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
